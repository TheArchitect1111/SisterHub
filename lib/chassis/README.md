# SisterHub chassis bindings

Tenant config lives in `sisterhub-portal.ts`. Shared runtime comes from `@ea/portal-chassis`.

After chassis changes in `ea-operating-system/portal-core`:

```bash
npm run sync-chassis
npm install
```
